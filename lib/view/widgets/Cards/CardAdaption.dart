import 'package:flutter/material.dart';

class CardAdaption extends StatelessWidget {
  const CardAdaption({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
